<%= form_for(@type_account) do |f| %>
  <% if @type_account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_account.errors.count, "error") %> prohibited this type_account from being saved:</h2>

      <ul>
      <% @type_account.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :type_account %><br>
    <%= f.text_field :type_account %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
