<%= form_for(@account) do |f| %>
  <% if @account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@account.errors.count, "error") %> prohibited this account from being saved:</h2>

      <ul>
      <% @account.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.collection_select(:id_type_account, TypeAccount.all, :id, :type_account) %><br>
    <%= f.label :name_account %><br>
    <%= f.text_field :name_account %>
  </div>
  <div class="field">
    <%= f.label :amount_account %><br>
    <%= f.text_field :amount_account %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
